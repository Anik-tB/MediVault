PORT=5000

DB_HOST=localhost
DB_PORT=5432
DB_NAME=medivault
DB_USER=postgres
DB_PASSWORD=your_postgres_password

# Optional: use this instead of the DB_* values above if you prefer one string.
# DATABASE_URL=postgresql://postgres:your_postgres_password@localhost:5432/medivault
